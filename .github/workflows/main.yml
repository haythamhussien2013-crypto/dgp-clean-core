name: DGP Clean Start

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Run Python Script
        run: |
          echo "🔥 STARTING DGP SYSTEM"
          python3 -c "
print('DGP SYSTEM ONLINE')
print('LEVEL 1 OK')
print('LEVEL 2 OK')
print('LEVEL 3 OK')
print
